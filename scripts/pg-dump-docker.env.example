# Copier vers scripts/.pg-dump-docker.env (non versionné) ou exporter dans le shell.
# URL Neon : endpoint direct, sslmode=require, et pour macOS sans SNI fiable :
#   &options=endpoint%3DVOTRE_ENDPOINT_ID
# Exemple d’endpoint : ep-old-hall-a432t5mz (partie avant .us-east-1… dans l’hôte)
#
# export DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&options=endpoint%3Dep-xxx"
